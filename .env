# Local default. Render injects PORT automatically.
PORT=3000
TZ=Europe/Istanbul
# Optional integrations:
# API_FOOTBALL_KEY=xxxxxxxxxxxxxxxxxxxxxxxxxxxx
# PREDICTIONS_FEED=https://yourdomain.com/predictions/YYYY-MM-DD.json
# Optional persistent disk on Render (Settings -> Disks -> mount at /data):
# DATA_DIR=/data
